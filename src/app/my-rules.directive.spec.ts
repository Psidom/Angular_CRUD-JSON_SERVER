import { MyRulesDirective } from './my-rules.directive';

describe('MyRulesDirective', () => {
  it('should create an instance', () => {
    const directive = new MyRulesDirective();
    expect(directive).toBeTruthy();
  });
});
